# GuardianShield Backend Environment Variables

HOST=0.0.0.0
PORT=8000
CORS_ORIGINS=["*"]

# Optional: Google Gemini API Key for Layer 3 Brain reasoning
# If left blank, deterministic heuristics fallback is automatically active
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
